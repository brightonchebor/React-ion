let selectedColors = ['red', 'blue'];

// console.log(selectedColors);
// console.log(selectedColors[0])

const greet = (name, firstName) => {
    console.log('Hello ' + name + ' ' + firstName);
}

console.log(greet('Brighton', 'Chebor'));